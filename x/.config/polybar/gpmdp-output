PLAYING=$(cat "$HOME/.config/Google Play Music Desktop Player/json_store/playback.json")
if [ -n "$PLAYING" ]; then
  echo $PLAYING | jq -r '.song.artist, .song.title' | sed ':a;N;$!ba;s/\n/ \- /'
fi
